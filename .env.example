# Optional. Without a key the app runs in replay mode — a recorded generation
# streamed through the exact same code path, at zero cost.
ANTHROPIC_API_KEY=

# Optional model override for live mode.
BRAND_MODEL=claude-opus-4-8
